

require('../bower_components/angular/angular.js');

const MODULE_NAME = 'myApp';

angular.module(MODULE_NAME, []);

export default MODULE_NAME;

// var req = require.context('./controllers/', true, /\.js$/);
// req([]);
